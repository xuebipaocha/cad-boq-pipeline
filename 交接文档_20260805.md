# CAD-BOQ Pipeline 交接文档（2026-08-05 晚）

> 本文件是给**新会话**的快速交接。新会话从这里开始，不要重新从零分析。
> 配套：SKILL.md（入口）+ 说明文档.md（权威完整版，v4.0~v5.15 全部迭代）

---

## 一、当前版本与状态

- **版本**: v5.15.1（面积防污染修复）
- **位置**: `D:\Reasonix Agent\项目1\cad-boq-pipeline\`
- **数据**: `data/liaoning_24.db`（27M，辽宁定额 29,839 条）+ `data/national_24.db`（2.5M，国标清单 3,734 条）+ `data/structure_rules.db`（24K，规范 12 条）
- **测试**: 单元 13/13、回归 8/8、构件 13/13、规范 6/6 全绿；基准 20 用例综合准确率 100%（VISION_OFF=1 跑）
- **核心能力**: 识图→审图→算量→编清单→组价 五步流水线；v5.15 识图必经视觉路径（qwen3.7-flash）与几何交叉验证（冲突标待核、不覆盖几何）；v5.12 全专业构件化；v5.14 质量聚合报告

## 二、进行中的任务（✅ 2026-08-05 已全部完成）

### 2.1 背景
数据库体检发现三类缺失，用**视觉模型读 PDF 表格页**回填——**已于 2026-08-05 晚完成**。

| 库 | 问题 | 补录前 | 补录后 | 说明 |
|---|---|---|---|---|
| liaoning_24.db | 基价 ≤0 | 577 | **60** | 残余全为费率类(脚手架/超高/增加费, 本就无基价) |
| liaoning_24.db | 名称空 | 13 | **0** | ✅ |
| liaoning_24.db | unit 空 | 2,536 | **2,373** | 费率类+PDF无单位列条目 |
| national_24.db | unit 覆盖率 | 56.1% | **94.9%** | 全本扫描 11 本计算标准 PDF |
| national_24.db | 占位名 `XX工程_编码` | 1,340 | **0** | 1,295 修复 + 45 幻影编码标待核 |
| national_24.db | 错位名 `按设计…计算` | 63 | **0** | 清零 |

**45 条幻影编码**：11 本计算标准 PDF 中均不存在（v4.0 OCR 幻觉），已标 `待核-幻影编码` 不编造。
**验证**：单元 13/13、回归 8/8、构件 13/13、规范 6/6、基准 20 用例 100% 全绿。

### 2.2 脚本（已完工，留档备查）

| 脚本 | 用途 |
|---|---|
| `full_backfill.py` | 问题页定位→渲染→视觉识别→回填（v2.2：16路并发/dpi120/线条预筛/rows双格式）|
| `round2_backfill.py` | 宽窗口(±5页)补扫残余 |
| `full_scan_list.py` | 11 本计算标准 PDF 全本扫描（891页，清幻影外全部占位名/错位名）|

启动命令（如需重跑，先还原 `data/backup_20260805/` 备份）：
```bash
cd "D:\Reasonix Agent\项目1\cad-boq-pipeline\scripts"
PYTHONUTF8=1 python full_backfill.py --concurrency 16
PYTHONUTF8=1 python full_scan_list.py
```

### 2.4 验证方式

```bash
cd scripts && python -c "
import sqlite3
con = sqlite3.connect('../data/national_24.db')
n = con.execute('SELECT COUNT(*) FROM standard_items').fetchone()[0]
has = con.execute(\"SELECT COUNT(*) FROM standard_items WHERE unit IS NOT NULL AND unit!=''\").fetchone()[0]
print(f'unit 覆盖率: {has}/{n} = {has/n*100:.1f}% (补录前 56.1%)')"
```

补录完成后：全量测试（test_core/regression_test/test_component_model/test_structure_rules）+ 基准 20 用例 + 更新说明文档 4.2 数据层健康度。

## 三、数据库其他已知问题（补录期间可顺手修）

| 问题 | 数量 | 说明 |
|---|---|---|
| 清单名称占位 `XX工程_编码`（如 `房屋建筑与装饰工程_010202004`） | 待查 | v4.0 重建漏掉的占位名，应从 PDF 补真名 |
| 清单名称 OCR 错位（`按设计图示...计算` 这种计算规则文字） | 待查 | 名称列混入规则文字 |
| 定额名称含 `待核基价` 标记 | 577 条内 | 补录完成后清除标记 |
| 清单 unit 缺失 | 385 → 0 | 2.1 主任务 |
| 映射表 list_quota_mapping | 1,847 条 | 键对齐可用（外部报告 23,447 条关联率 0% 不可用，本库已超越） |

体检脚本（修复 REGEXP 后重跑）：
```python
# 占位名
SELECT COUNT(*) FROM standard_items WHERE item_name LIKE '%\_%' ESCAPE '\' AND item_name GLOB '*_[0-9]*'
# 计算规则错位
SELECT COUNT(*) FROM standard_items WHERE item_name LIKE '按设计%' OR item_name LIKE '计算规则%'
```

## 四、环境与配置

- **千问 API**：Reasonix 全局 `C:\Users\32865\AppData\Roaming\reasonix\.env` 的 `QWEN_API_KEY`；config.toml `qwen-cn` provider（base_url=`https://dashscope.aliyuncs.com/compatible-mode/v1`，默认模型 `qwen3.7-flash`）
- **调用封装**：`scripts/vision_query.py`（query_vision(png, enable, prompt)）
- **视觉开关**：`VISION_OFF=1` 关闭视觉（测试/离线用）
- **PDF 源文件**：
  - 定额：`D:\用户文件\Desktop\2024年辽宁省建设工程计价依据\`（A房屋建筑与装饰.pdf 1612 页等 12 本）
  - 清单：`D:\用户文件\Desktop\2024建设工程工程量清单计价标准+工程量计算标准\`（11 本）
- **依赖**：ezdxf / openpyxl / matplotlib / pymupdf(fitz) / playwright(已弃用)

## 五、工作流（用户偏好，必须遵守）

1. **识图必经视觉路径** + 与几何逻辑交叉验证（VISION_OFF 可关闭）；非识图场景不调视觉（省 token）
2. 视觉结果仅作交叉验证信号：一致→印证；冲突→标"待核"；**永不覆盖几何结果**
3. 基准 20 用例全部程序生成图（真实图纸 v5.11 已移除，防单项目噪声污染）
4. 价格只保留定额价（信息价 v5.11 已删）
5. 文档只有一个权威：**说明文档.md**（不要再新建变更说明文档）

## 六、近期版本脉络（快速回忆）

- v5.11 清理（去真实图纸/去信息价）→ v5.12 全专业构件化（市政/园林/安装/精装）→ v5.13 面积统一+渲染层 → v5.14 pipeline 瘦身（--steps/merge_views CLI/质量聚合报告/db 收口）→ v5.15 视觉交叉验证（识图必经视觉）→ v5.15.1 面积防污染修复

## 七、本次会话已做但未收尾的

- full_backfill.py v2.1 优化完成（并发 8/dpi 120/线条预筛/rows 格式修复），**已验证单页提取**（定额页 4 行全字段、清单页 4 行），**未全量跑**
- 数据库体检初步发现：占位名/OCR 错位/待核标记待修（见三）
- 用户中途要求"暂停换窗口做交接文档"，补录待新窗口续跑
