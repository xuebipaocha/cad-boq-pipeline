<div align="center">

# CAD-BOQ Pipeline

**CAD 图纸 → 工程量清单（BOQ）全流程自动化工作流**

识图 → 审图 → 算量 → 编清单 → 组价

</div>

---

## ✨ 功能特性

- **五步流水线**：识图 → 审图（24 条规则）→ 算量（构件模型）→ 编清单（清单-定额映射）→ 组价（取费链）
- **5 专业覆盖**：房屋建筑与装饰 / 安装 / 市政 / 园林绿化 / 钢结构
- **全专业构件化**：24 类构件（柱梁板墙房间、安装 13 类分系统、钢构、市政道路、园林苗木等）
- **工程性质感知**：自动判定新建 / 大修与改造（大修走专属计算器：拆除/翻新/更换）
- **识图必经视觉路径**：渲染 → 多模态模型（qwen3.7-flash）视觉识别 → 与几何识图交叉验证（冲突标待核，视觉永不覆盖几何）
- **数据可信度卡口**：防"编造"——数字三要素校验、来源四级、待提取项不进清单
- **审图规则 24 条**：编码格式 / 量纲 / 价格 / 必填字段 / 跨表一致 / 规范条文（GB 50010 等）
- **多视图支持**：平/立/剖多张图纸自动合并去重，工程性质汇总
- **0 量占位卡口**：算量阶段剔除占位项，独立交付待提取清单
- **质量聚合报告**：审图/算量/清单三环节质量合一

## 📦 内置数据

| 数据 | 规模 | 说明 |
|:--|:--|:--|
| 辽宁 24 定额 | 29,839 条 | 官方 PDF 重建，主键零重复；基价≤0 仅 60 条（费率类，本就无基价） |
| 国标 24 清单 | 3,734 条 | 真实名称（占位名 1,340 已清零），unit 覆盖率 94.9% |
| 清单-定额映射 | 1,847 条 | 键已对齐（6 位→9 位编码） |
| 规范知识库 | 12 条 | GB 50010 等（梁高跨比/配筋率/保护层/板厚跨比） |

> 价格仅保留定额价（基价/人工/材料/机械），不依赖市场信息价。
> 2026-08-05 视觉补录：清单 unit 覆盖率 56.1%→94.9%、占位名 1,340→0、错位名 63→0、定额基价 577→60。

## 🚀 快速开始

```bash
# 依赖
pip install -r requirements.txt    # ezdxf, openpyxl, matplotlib

cd scripts

# 全流程（单图）
python pipeline.py 图纸.dxf --steps 识图,审图,算量,编清单,组价

# 多视图（平立剖多张，自动合并）
python pipeline.py 平面.dxf 立面.dxf 剖面.dxf --steps 识图,审图,算量,编清单,组价

# 指定专业 / 出渲染图
python pipeline.py 图纸.dxf --specialty 市政工程 --render
```

### 视觉识别（可选，识图默认必经）

```bash
# 需要配置 QWEN_API_KEY（阿里云百炼，qwen3.7-flash）
# 识图自动：渲染 → 视觉识别 → 与几何交叉验证
python pipeline.py 图纸.dxf --steps 识图,审图

# 临时关闭视觉（离线/无 Key）
VISION_OFF=1 python pipeline.py 图纸.dxf

# 独立视觉查询
python vision_query.py 渲染.png --enable-vision
```

## 🧪 测试

```bash
python test_core.py             # 单元测试 13/13
python regression_test.py       # 回归测试 8/8
python test_component_model.py  # 构件模型 13/13
python test_structure_rules.py  # 规范规则 6/6
python benchmark_accuracy.py    # 基准 20 用例 100%
```

## 📁 项目结构

```
cad-boq-pipeline/
├── SKILL.md               # Skill 入口
├── 说明文档.md             # 唯一权威完整说明（架构/能力评估/版本记录）
├── scripts/               # 五步流水线 + 视觉路径 + 测试
│   ├── pipeline.py        # 调度器（--steps/QC 断言）
│   ├── step1_recognize.py # 识图（+视觉路径接入）
│   ├── step2_review.py    # 审图（24 条规则）
│   ├── step3_calculate.py # 算量（构件模型 + 0量卡口）
│   ├── step4_compile_boq.py  # 编清单
│   ├── step5_price.py     # 组价（定额价）
│   ├── vision_fusion.py   # 视觉交叉验证（方案C二次复核）
│   ├── vision_query.py    # 视觉识别封装（qwen3.7-flash）
│   ├── render_dxf.py      # DXF→PNG 渲染层
│   └── ...（42 个模块）
├── data/                  # 定额/清单/映射/规范库
├── benchmarks/cases/      # 20 个基准用例（五专业）
├── test_data/             # 测试图纸
└── requirements.txt
```

## 📜 版本记录

| 版本 | 里程碑 |
|:--|:--|
| v4.0 | 数据层重建（定额 29,839 条零重复 / 单位标准化 183→18）|
| v5.0 | 构件级建模（七路证据融合引擎）|
| v5.6 | 数据可信度卡口（防编造）|
| v5.11 | 清理（去真实图纸用例 / 去信息价）|
| v5.12 | 全专业构件化（市政/园林/安装分系统/精装）|
| v5.13 | 面积裁决统一 / 0 量卡口 / 渲染层 |
| v5.14 | 工作流架构（db 收口 / schema_version / 调度瘦身 / 质量聚合）|
| v5.15 | 识图必经视觉路径 + 交叉验证（方案C二次复核）|
| v5.15.1 | 面积裁决防污染修复 |
| v5.15.2 | 视觉重读 11 本计算标准 PDF 补录清单 unit |
| v5.15.3 | 数据库视觉补录完成：清单 unit 94.9% / 占位名清零 / 错位名清零 / 定额基价 577→60 |

## ⚠️ 说明

- 数据来自官方 PDF 重建，**含版权内容**，公开使用请注意合规
- 清单 unit 覆盖率 94.9%（残余为 PDF 无表格页条目）、45 条幻影编码标待核、60 条定额基价为费率类（正常）——详见 `说明文档.md` 第 4 节
- 视觉识别需自行申请阿里云百炼 API Key（`QWEN_API_KEY` 环境变量）

## 📄 License

详见 `说明文档.md`（当前未指定开源协议，联系作者获取授权）
