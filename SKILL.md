---
name: cad-boq-pipeline
version: 6.2.0
description: >
  CAD-BOQ 全流程工作流：识图→审图→算量→编清单→组价。
  内置辽宁24定额(29,839条,零重复主键)和国标24清单(3,734条真实名称)数据库。
  支持5个专业：房屋建筑与装饰/安装/市政/园林绿化/钢结构。
  工程性质感知(新建/大修与改造)、构件级建模、规范知识库审查、
  数据可信度卡口(防编造)、视觉优先交叉验证(置信度高时视觉优先)、依赖健康自检。
  v6.2：设计说明专项解析(材料规格/做法层次/施工范围/工程概况)；v6.1：措施规则+房间分区；
  v6.0：构件扣减/门窗表/配线/栅格图结构化；
  v5.19：主材价格库(真实价优先)；v5.18：两段式调用(仅Excel输出)。
  完整说明见 说明文档.md（整合 v4.0~v6.2 全部迭代）。
---

# CAD-BOQ Pipeline v6.2.0

> 唯一权威说明：**说明文档.md**（版本演进/架构/能力评估/使用方式）
> 版本记录：v4.0 数据层重建 → v5.0 构件建模 → v5.6 可信度卡口 → v5.11 清理
> → v5.12 全专业构件化 → v5.14 工作流架构 → v5.15 视觉交叉验证 → v5.16 多定额组合
> → v5.17 特征驱动套定额 → v5.18 两段式调用 → v5.19 主材价格库 → v6.0 识图算量深化 → v6.1 措施规则/房间分区

## 核心能力速览

| 能力 | 状态 |
|:--|:--|
| 五步流水线（识图→审图→算量→编清单→组价）| ✅ |
| 5 专业（房建/安装/市政/园林/钢构）| ✅ |
| 构件级建模（全专业 24 类构件）| ✅ v5.0/v5.12 |
| 工程性质感知（新建/大修与改造）| ✅ v5.4 |
| 数据可信度卡口（防编造）| ✅ v5.6 |
| 24 条审图规则 + 规范知识库 | ✅ v5.0/v5.14 |
| 识图必经视觉路径 + 交叉验证（v6.0 视觉优先）| ✅ v5.15/v6.0 |
| 构件扣减（板洞/墙门窗/梁柱）| ✅ v6.0 |
| 门窗表提取 / 文字标注 / 做法表多层 | ✅ v6.0 |
| 电气配线（BV/BYJ×配管×根数）| ✅ v6.0 |
| 栅格图视觉读图结构化 | ✅ v6.0 |
| 措施项目规则表（10 项）| ✅ v6.1 |
| 房间分区几何化（实际分区量）| ✅ v6.1 |
| 设计说明专项解析（材料规格/做法/概况）| ✅ v6.2 |
| 0 量占位卡口 | ✅ v5.13 |
| 测试：回归 8/8、单元 13/13、构件 13/13、规范 6/6、基准 20 用例 100% | ✅ |

## 快速开始

```bash
cd scripts

# ① 识图算量段（默认，不含价格）——只出工程量清单(无价格)、工程量计算书、图纸疑问
python pipeline.py 图纸.dxf --steps 识图,审图,算量,编清单

# ② 组价段（明确要求时才调用）——在前者基础上追加组价, 出含价格清单+综合单价分析
python pipeline.py 图纸.dxf --steps 识图,审图,算量,编清单,组价
python pipeline.py 图纸.dxf --steps 编清单,组价              # 已有算量结果时只做清单+组价

# 多视图
python pipeline.py 平面.dxf 立面.dxf 剖面.dxf --steps 识图,审图,算量,编清单

# 输出目录仅保留 Excel 成果(中间 JSON 自动清理); 调试需保留 JSON 时加 --keep-json
python pipeline.py 图纸.dxf --steps 识图,审图,算量,编清单,组价 --keep-json

# 测试
python test_core.py && python regression_test.py                  # 测试
python benchmark_accuracy.py                                      # 基准
```

## 关键纪律（用户确认）

1. 识图必经视觉路径，视觉与几何交叉验证；**视觉置信度≥几何且≥0.7 时视觉优先**（v6.0），否则冲突标待核；**交叉验证步骤不能少**
2. 主力用纯文本模型；视觉（qwen3.7-flash）仅识图按需调用，`VISION_OFF=1` 关闭
3. **识图算量只出不含价格的工程量清单、工程量计算表和图纸疑问；只有要求组价时才调用组价模块做定额组价**（组价是独立步骤，不会在算量段自动触发）
4. 价格仅保留定额价（v5.11 移除信息价）；定额选取按清单项目特征驱动，本册无合适定额跨册借用（借），再无则自补定额（补）；国标清单无合适项自补清单（B类编码）
5. 输出成果仅 4 个 Excel（图纸问题清单 / 工程量计算书 / 分部分项工程量清单计价表 / 已组价清单），中间 JSON 自动清理（`--keep-json` 保留）
