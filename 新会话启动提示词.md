# 新会话启动提示词（复制到新窗口第一条消息）

你是 cad-boq-pipeline 项目的开发助理。先读交接文档，再干活。

## 启动指令

```
先读取 D:\Reasonix Agent\项目1\cad-boq-pipeline\交接文档_20260805.md 和 SKILL.md，
了解项目当前状态。然后按以下优先级执行：

1.【续跑补录】启动数据库补录：
   cd "D:\Reasonix Agent\项目1\cad-boq-pipeline\scripts"
   PYTHONUTF8=1 python full_backfill.py
   （后台运行 run_in_background=true，8路并发，预计40-60分钟；
   每10页自动写进度 data/full_backfill_progress.json，可随时kill续跑；
   若 API 限流(403)自动降并发重试）
   跑完验证 unit 覆盖率提升，全量测试+基准 20 用例回归。

2.【修数据库问题】补录期间顺手修（用视觉/规则修复，改前先备份 db）：
   - 清单名称占位"XX工程_编码"→从PDF补真名
   - 清单名称 OCR 错位（"按设计图示…计算"）→ 清理
   - 定额基价<=0 的 577 条 → 补录回填
   - 补录完成后清除"待核基价"标记

3.【文档同步】全部完成后更新 说明文档.md 的 4.2 数据层健康度，
   删除本交接文档（或标注已完成）。

约束：
- 识图必经视觉路径+几何交叉验证（VISION_OFF=1 可关，测试用）
- 非识图场景不调视觉模型（省 token）
- 文档只有一个权威：说明文档.md，不要新建变更说明
- 基准 20 用例必须全绿才算完成
```

## 若你不想我自动跑，只想手动控制

```
先读交接文档，然后等我指令。当前重点是：
1. full_backfill.py 补录（命令见交接文档 2.3）
2. 数据库三处问题修复（交接文档三）
3. 完成后回归+文档更新
```

## 常用命令速查

```bash
# 全流程
cd "D:\Reasonix Agent\项目1\cad-boq-pipeline\scripts"
PYTHONUTF8=1 python pipeline.py <dxf路径> --steps 识图,审图,算量,编清单,组价 --output-dir <输出目录>

# 测试
VISION_OFF=1 PYTHONUTF8=1 python test_core.py
VISION_OFF=1 PYTHONUTF8=1 python regression_test.py
VISION_OFF=1 PYTHONUTF8=1 python test_component_model.py
VISION_OFF=1 PYTHONUTF8=1 python test_structure_rules.py
VISION_OFF=1 PYTHONUTF8=1 python benchmark_accuracy.py

# 补录
PYTHONUTF8=1 python full_backfill.py            # 8路并发全量
PYTHONUTF8=1 python full_backfill.py --dry-run  # 只扫不写
```
